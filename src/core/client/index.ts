/**
 * Registered external services (to be accessed via the service gateway).
 */
export enum ExternalService {

  RESOURCE_SERVER = "lsrs",
  LOG_PROCESSOR = "tlp",
  TRANSLATION_MANAGER = "tms",
  STACK_ADMIN_SERVICE = "lsas",
  STACK_ADMIN_SERVICE_JS = "lsas_js",
  FAILOVER = "cbfs"
}
